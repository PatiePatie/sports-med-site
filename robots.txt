# Vitalité Plan — robots.txt
# ---------------------------------------------------------------
# Served from origin via Cloudflare — verified live at the edge.
# ALL signals explicitly ALLOW — the site is public, AI-accessible,
# and crawlable. No restrictions, no ambiguity.
# ---------------------------------------------------------------

User-agent: *
Allow: /

# Content signals (content-signals.org) — explicit, no ambiguity:
# search:   building a search index / providing search results
# ai-input: feeding content into AI models in real time (RAG, grounding,
#           Gemini/Claude link access, AI search answers)
# ai-train: training or fine-tuning AI models
#
# Policy: ALL ALLOWED. NOTE: Gemini link access runs on Google-Extended,
# which reads ai-train — setting it to disallow blocks Gemini retrieval.
# If training opt-out is needed later, that tradeoff applies.

search: allow
ai-input: allow
ai-train: allow

Sitemap: https://vitaliteplan.com/sitemap.xml